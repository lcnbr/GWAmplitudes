* Qgraf graphML style file

<prologue>
<<?xml version="1.0" encoding="UTF-8"?>>
<<graphml xmlns="http://graphml.graphdrawing.org/xmlns">>
<<!--
File generated by <program>
For use with any GraphML compatible input
Style by Lucien Huber 30.06.2022
================================   
-->>    


<<!-- Input model: out 
<command_loop> <command_line_loop><command_data><back><end>

<end>
-->>
<<key id="dIndx"    for="graph" attr.name="diagram_index"     attr.type="int"/>>
<<key id="legs"     for="graph" attr.name="legs"              attr.type="int"/>>
<<key id="legsIn"   for="graph" attr.name="legs_in"           attr.type="int"/>>
<<key id="legsOut"  for="graph" attr.name="legs_out"          attr.type="int"/>>
<<key id="loops"    for="graph" attr.name="loops"             attr.type="int"/>>
<<key id="props"    for="graph" attr.name="propagators"       attr.type="int"/
<back>>> 
<<key id="sign"     for="graph" attr.name="sign"              attr.
<back>type="string"/>>
<<key id="symm"     for="graph" attr.name="symmetry_number"   attr.type="int"/>>
<<key id="vertices" for="graph" attr.name="vertices"          attr.type="int"/>>
  
<<!-- Edge attributes -->>
<<key id="f" for="edge" attr.name="field"       attr.type="string"/>>
<<key id="dF" for="edge" attr.name="dual-field"  attr.type="string"/>> 
<<key id="mom" for="edge" attr.name="momentum" attr.type="string"/>>
<<key id="dMom" for="edge" attr.name="dual-momentum" attr.type="string"/>>

<<key id="fIndx" for="edge" attr.name="field_index" attr.type="int"/>>
<<key id="fType" for="edge" attr.name="field_type" attr.type="int"/>>
<<key id="fSign" for="edge" attr.name="field_sign" attr.type="string"/>>

<<key id="rIndx" for="edge" attr.name="ray_index" attr.type="int"/>>
<<key id="vIndx" for="edge" attr.name="vertex_index" attr.type="int"/>>
<<key id="vDeg" for="edge" attr.name="vertex_degree" attr.type="int"/>>
<<!-- Internal edges / propagators

<<dual-field>>          | the name of the field phi_2i (e.g. positron )
<<dual-field index>>    | the unsigned integer 2i ( e.g. 10 )
<<dual-momentum>>       | the symmetri of <<momentum>>( e.g. -k1-k2 )
<<dual-ray index>>      | the ray index of the field phi_2i ( e.g. 1 )
<<dual-vertex degree>>  | the degree of the interaction vertex to which the 
  field phi_2i belongs ( e.g. 3 )
<<dual-vertex index>>   | the vertex index of the interaction vertex to which 
  phi_2i belongs ( e.g. 3 )
<<field>>               | the name of the field phi_2i 1 ( e.g. electron )
<<field index>>         | the unsigned integer 2i 1 ( e.g. 9 )
<<field type>>          | the field type of the field phi_2i-1 , which is 
  always equal to 3 ( e.g. 3 )
<<field sign>>          | the sign of propagator i ( e.g. - )
<<momentum>>            | the momentum of propagator i ( e.g. k1+k2 )
<<propagator index>>    | the unsigned integer i ( e.g. 5 )
<<ray index>>           | the ray index of the field phi_2i-1 ( e.g. 2 )
<<vertex degree>>       | the degree of the vertex that ontains phi_2i-1 
(e.g. 3)
<<vertex index>>        | the vertex index of the vertex that ontains phi_2i 1 
( e.g. 4 )
 -->>
<<key id="pIndx" for="edge" attr.name="propagator_index" attr.type="int"/>>
<<key id="dFIndx" for="edge" attr.name="dual-field_index" attr.type="int"/>>

<<key id="dRIndx" for="edge" attr.name="dual-ray_index" attr.type="int"/>>
<<key id="dVDeg" for="edge" attr.name="dual-vertex_degree" attr.type="int"/>>
<<key id="dVIndx" for="edge" attr.name="dual-vertex_index" attr.type="int"/>>

<<!-- External edges/legs 

there are two types of leg loops, the incoming loop and the outgoing
loop. In the former case the loop is executed r times, once for each incoming 
field, while in the latter case the loop is run s times, once for each outgoing 
field (the legs of the diagram are, rather obviously, the main objects that are 
acessed in those loops).

<<dual-field>>      | the conjugate of <<field>> (e.g. positron )
<<dual-momentum>>   | the symmetric of <<momentum>> (e.g. -p1 )
<<field>>           | the name of phiin_i (e.g. electron )
<<field index>>     | the field index of phiin_i (e.g. -1 )
<<field type>>      | the field type of phiin_i (e.g. 1 )
<<field sign>>      | the sign of phiin_i (e.g. - )
<<in index>>        | the unsigned integer i (e.g. 1 )
<<out index>>       | the unsigned integer i (e.g. 1 )
<<momentum>>        | the momentum owing into the diagram through leg i (e.g. 
  p1 )
<<ray index>>       | the unsigned integer j , which is the ray index of phi_k;
  j (e.g. 2 )
<<vertex degree>>   | the vertex degree of v_k (e.g. 3 )
<<vertex index>>    | the vertex index of v_k , which is the unsigned integer k 
  (e.g. 1 )
<<leg index>>       | the unsigned integer r +i (e.e 2 )
-->>
<<key id="inIndx"   for="edge" attr.name="in_index" attr.type="int"/>>
<<key id="outIndx"  for="edge" attr.name="out_index" attr.type="int"/>>
<<key id="legIndx"  for="edge" attr.name="leg_index" attr.type="int"/>>

<<!-- field and prop functions are given by the model file, and can encde 
style, and addtional attibutes  
e.g. [[fieldtype]] could be S (scalar), V (vector), T (tensor), etc.
we then would need to define this as an attribute e.g.:

<<key id="fieldtype" for="edge" attr.name="fieldtype" attr.type="string"/>>

-->>

<<!-- Vertex attributes
<<vertex degree>> | the degree of vertex v_i (e.g. 3 )
<<vertex index>>  | the unsigned integer i (e.g. 1 )
-->>
<<key id="vDeg" for="vertex" attr.name="vertex_degree" attr.type="int"/>>
<<key id="vIndx" for="vertex" attr.name="vertex_index" attr.type="int"/>>



<<!-- Diagrams  -->>    
<diagram>



<<!-- Diagram number <diagram_index> with <legs> external legs, 
  <legs_in> of which are incoming
  <legs_out> of which are outgoing
Containg <loops> loops,
with <propagators> internal lines,
with sign <sign>,
with symmetry factor 1/<symmetry_number> we encode the symmetry number,
with <vertices> vertices -->>
<<graph id="diag<diagram_index>" edgedefault="directed">>
<<data key="dIndx">><diagram_index><</data>>
<<data key="legs">><legs><</data>>
<<data key="legsIn">><legs_in><</data>>
<<data key="legsOut">><legs_out><</data>>
<<data key="loops">><loops><</data>>
<<data key="props">><propagators><</data>>
<<data key="sign">>"<sign>"<</data>>
<<data key="symm">><symmetry_number><</data>>
<<data key="vertices">><vertices><</data>>


<in_loop> 
<<node id="v_in<in_index>">>
<<data key="vDeg">>1<</data>>
<<data key="vIndx">><in_index><</data>>
<</node>> 

<<edge id="leg_in<in_index>" source="v_in<in_index>" 
 target="v_loop<vertex_index>">>
<<data key="inIndx">><in_index><</data>>
<<data key="legIndx">><in_index><</data>>

<<data key="f">>"<field>"<</data>>
<<data key="dF">>"<dual-field>"<</data>>
<<data key="mom">>"<momentum>"<</data>>
<<data key="dMom">>"<dual-momentum>"<</data>>

<<data key="fIndx">><field_index><</data>>
<<data key="fType">><field_type><</data>>
<<data key="fSign">>"<field_sign>"<</data>>

<<data key="rIndx">><ray_index><</data>>
<<data key="vIndx">><vertex_index><</data>>
<<data key="vDeg">><vertex_degree><</data>>
<</edge>> 

<end>
<out_loop> 

<<node id="v_out<out_index>">>
<<data key="vDeg">>1<</data>>
<<data key="vIndx">><out_index><</data>>
<</node>> 

<<edge id="leg_out<out_index>" source="v_out<out_index>" 
 target="v_loop<vertex_index>">>
<<data key="outIndx">><out_index><</data>>
<<data key="legIndx">><leg_index><</data>>

<<data key="f">>"<field>"<</data>>
<<data key="dF">>"<dual-field>"<</data>>
<<data key="mom">>"<momentum>"<</data>>
<<data key="dMom">>"<dual-momentum>"<</data>>
    
<<data key="fIndx">><field_index><</data>>
<<data key="fType">><field_type><</data>>
<<data key="fSign">>"<field_sign>"<</data>>
    
<<data key="rIndx">><ray_index><</data>>
<<data key="vIndx">><vertex_index><</data>>
<<data key="vDeg">><vertex_degree><</data>>
<</edge>> 

<end>
<propagator_loop> 
<<edge id="prop<propagator_index>"  
source="v_loop<dual-vertex_index>" target="v_loop<vertex_index>">>

<<data key="f">>"<field>"<</data>>
<<data key="dF">>"<dual-field>"<</data>>
<<data key="mom">>"<momentum>"<</data>>
<<data key="dMom">>"<dual-momentum>"<</data>>
    
<<data key="fIndx">><field_index><</data>>
<<data key="fType">><field_type><</data>>
<<data key="fSign">>"<field_sign>"<</data>>
    
<<data key="rIndx">><ray_index><</data>>
<<data key="vIndx">><vertex_index><</data>>
<<data key="vDeg">><vertex_degree><</data>>

<<data key="pIndx">><propagator_index><</data>>
<<data key="dFIndx">><dual-field_index><</data>>

<<data key="dRIndx">><dual-ray_index><</data>>
<<data key="dVDeg">><dual-vertex_degree><</data>>
<<data key="dVIndx">><dual-vertex_index><</data>>
<</edge>>
<end>
<vertex_loop>
<<node id="v_loop<vertex_index>">>
<<data key="vDeg">><vertex_degree><</data>>
<<data key="vIndx">><vertex_index><</data>>
<</node>>
<end>    



<</graphml>>             


            

<epilogue>
<<!-- Total number of diagrams: <diagram_index> -->>

<exit>